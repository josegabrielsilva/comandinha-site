DEPLOY AUTOMATIZADO (recomendado)
=================================

LOCAL (uma vez):
  1. brew install lftp
  2. cp .env.deploy.example .env.deploy
  3. Preencha FTP_HOST, FTP_USER, FTP_PASSWORD (cPanel → FTP Accounts)

PUBLICAR:
  npm run deploy

GITHUB ACTIONS (recomendado — credenciais no GitHub):
  Repo → Settings → Secrets and variables → Actions → New repository secret

  Obrigatórios:
    FTP_HOST      → hostname do SERVIDOR no cPanel (ex: br1234.hostgator.com.br)
                    NÃO use ftp.usecomandinha.com.br (Cloudflare bloqueia no CI)
    FTP_USER      → conta FTP (ex: github@usecomandinha.com.br)
    FTP_PASSWORD  → senha da conta FTP
    FTP_PORT      → 21 (FTPS explícito; opcional, padrão 21)

  cPanel → Contas de FTP → diretório da conta = public_html
    Caminho: /home1/SEU_USUARIO/public_html
    NÃO use usecomandinha.com.br/github (essa pasta não é o site público).

  Opcional (só se a conta já abrir dentro de public_html):
    FTP_ASSUME_CHROOT_PUBLIC_HTML → true

  Opcionais (purge automático do Cloudflare):
    CLOUDFLARE_ZONE_ID
    CLOUDFLARE_API_TOKEN

  Dispara em push na main (quando HTML/CSS/JS/assets mudam).
  Ou manualmente: Actions → Deploy HostGator → Run workflow.

---

UPLOAD MANUAL (legado — File Manager / arrastar arquivos)
  Suba em public_html: index.html, privacidade.html, termos.html, .htaccess, css/, js/, assets/

CLOUDFLARE
  Se o site parecer antigo após o deploy, purgue o cache:
  dash.cloudflare.com → Caching → Purge Everything
  (o script npm run deploy e o GitHub Action fazem isso se CLOUDFLARE_* estiver configurado)

CONFERIR
  https://usecomandinha.com.br/css/site.css?v=2
  https://usecomandinha.com.br/assets/screens/screen-comandas.png?v=2

Apague em public_html/assets/ se ainda existirem:
  comandinha-icon.svg, screen-cardapio.png
